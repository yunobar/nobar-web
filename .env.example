# Base URL for the album backend API. Optional — defaults to
# "http://localhost:8080/api/v1". The backend allows CORS from the dev origin
# (localhost:5173) with credentials, so the frontend calls it directly.
VITE_API_BASE_URL=http://localhost:8080/api/v1
